---
'manifest': patch
---

Stop publishing `docs/github-sdk.png` in the npm package. The architecture diagram is still in the repo and the README now references it by URL, cutting the tarball from 5.8 MB to about 123 kB.
