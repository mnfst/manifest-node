# Changesets

Run `npm run changeset` in a pull request that changes the published package.
Choose the Semantic Versioning bump and describe the user-visible change.

The release workflow collects pending changesets in a rolling release pull request.
Merging that release pull request updates the package version and changelog.
